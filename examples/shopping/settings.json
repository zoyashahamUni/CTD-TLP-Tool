{
  "factors": [
    {
      "name": "a_add_then_remove",
      "values": [
        {
          "value": "add_remove",
          "ltl": "F((step = add) & X(step = remove))"
        }
      ]
    },
    {
      "name": "b_checkout_then_idle",
      "values": [
        {
          "value": "checkout_idle",
          "ltl": "F((step = checkout) & X(step = idle))"
        }
      ]
    },
    {
      "name": "c_next_add",
      "values": [
        {
          "value": "next_add",
          "ltl": "X(step = add)"
        }
      ]
    }
  ],
  "step_var": "step",
  "end_flag": "end_of_test",
  "test_rule": "F(end_flag)"
}